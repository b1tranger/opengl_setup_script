# Chat Conversation

Note: _This is purely the output of the chat conversation and does not contain any raw data, codebase snippets, etc. used to generate the output._

### User Input

@[c:\Users\gsmur\Documents\GitHub\[personal]\opengl_setup_script\PROJECTS\create_project.bat] should create a blank project files, instead of project with old code from @[c:\Users\gsmur\Documents\GitHub\[personal]\opengl_setup_script\sample_projects\Lab0_Basic_Window] 

### AI Thinking & Execution Process

1. **Investigating Project Generation & Template Structure**:
   - Inspected `PROJECTS/create_project.bat` to see how new projects are generated.
   - Identified that `create_project.bat` had `TEMPLATE_DIR=%ROOT_DIR%\sample_projects\Lab0_Basic_Window` and copied `%TEMPLATE_DIR%\src\main.cpp`.
   - Inspected `sample_projects/Lab0_Basic_Window/src/main.cpp` and identified that it contained 180+ lines of 2D House Graphics code (roof, yellow house body, brown door, windows, 27 vertices, shader program with custom colors) rather than a clean, blank Modern OpenGL starter boilerplate.

2. **Designing a Dedicated Blank Template**:
   - Created a clean, robust, dedicated template folder `Dependencies/template/` containing:
     - `include/` (`glad.h`, `glfw3.h`, `khrplatform.h`)
     - `lib/` (`glfw3.dll`)
     - `src/glad.c`
     - `src/main.cpp` (Clean Modern OpenGL 3.3 Core Profile boilerplate with GLFW window initialization, GLAD loader, window resize callback, ESC key handling, clean background clear loop, and resource termination).
     - `Makefile` (Standard `make win` and `make linux` targets).

3. **Updating `PROJECTS/create_project.bat`**:
   - Set `TEMPLATE_DIR=%ROOT_DIR%\Dependencies\template` with fallback to `sample_projects\Lab0_Basic_Window`.
   - Set `BLANK_SRC=%ROOT_DIR%\Dependencies\template\src\main.cpp`.
   - Updated Step 4 to generate/copy the blank starter `src\main.cpp` from `Dependencies\template\src\main.cpp`.
   - Updated output messages to indicate blank project initialization.

4. **Testing & Verification**:
   - Executed `PROJECTS/create_project.bat BlankTestProject` via PowerShell.
   - Verified that `PROJECTS/BlankTestProject/src/main.cpp` was generated with pristine blank starter code (75 lines) and verified folder structure (`build/`, `include/`, `lib/`, `src/`, `Makefile`).
   - Cleaned up the test project.
   - Updated `README.md` directory layout tree to document `Dependencies/template/`.

### Assistant Response

The project creation script has been updated so that newly generated projects start with clean, blank Modern OpenGL boilerplate rather than the 2D House Graphics code from `sample_projects\Lab0_Basic_Window`.

### Summary of Changes

1. **Created Dedicated Blank Starter Template ([`Dependencies/template`](file:///c:/Users/gsmur/Documents/GitHub/[personal]/opengl_setup_script/Dependencies/template)):**
   - **[`src/main.cpp`](file:///c:/Users/gsmur/Documents/GitHub/[personal]/opengl_setup_script/Dependencies/template/src/main.cpp)**: A pristine Modern OpenGL (v3.3 Core Profile) starter file containing:
     - Window initialization (800x600) and GLAD function pointer loading.
     - Framebuffer resizing callback (`glViewport`).
     - Key event handling (press `ESC` to close).
     - Clean render loop with clear color (`glClearColor(0.1f, 0.1f, 0.15f, 1.0f); glClear(GL_COLOR_BUFFER_BIT);`).
     - Proper resource cleanup (`glfwTerminate()`).
     - Zero hardcoded geometries or custom assignment shaders.
   - **Build dependencies**: Bundles the required `include/` headers (`glad.h`, `glfw3.h`, `khrplatform.h`), `lib/glfw3.dll`, `src/glad.c`, and `Makefile`.

2. **Updated Project Scaffolding Script ([`PROJECTS/create_project.bat`](file:///c:/Users/gsmur/Documents/GitHub/[personal]/opengl_setup_script/PROJECTS/create_project.bat)):**
   - Configured `TEMPLATE_DIR` and `BLANK_SRC` to point to `Dependencies\template\`.
   - Generates the clean blank `src\main.cpp` for any new project created interactively or via CLI (`create_project.bat "Lab_Name"`).

3. **Documentation Update ([`README.md`](file:///c:/Users/gsmur/Documents/GitHub/[personal]/opengl_setup_script/README.md)):**
   - Added `Dependencies/template/` to the repository directory layout tree.
